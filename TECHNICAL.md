# celclean 技术方案

> 面向想了解实现、参数语义、验证方法与数据的读者。用户向说明见 [README.md](README.md)。
> 设计决策的来龙去脉（哪些方案被实测否掉、独立审核发现的缺陷、各版本变更）见本文第 11 节。
> 本文所有数字都是本机实测；标注 **[推断]** 的才是推断。

## 目录

1. [问题定义与核心取舍](#1-问题定义与核心取舍)
2. [算法流水线](#2-算法流水线)
3. [参数语义与杠杆方向](#3-参数语义与杠杆方向)
4. [验收口径（我们怎么量"更好"）](#4-验收口径我们怎么量更好)
5. [实测数据](#5-实测数据)
6. [独立验证：预训练模型交叉验证](#6-独立验证预训练模型交叉验证)
7. [与同类工具 / 通用去噪的实测对比](#7-与同类工具--通用去噪的实测对比)
8. [性能](#8-性能)
9. [已知失效模式与边界](#9-已知失效模式与边界)
10. [复现](#10-复现)
11. [设计演进与变更记录](#11-设计演进与变更记录)

---

## 1. 问题定义与核心取舍

**输入**：AI 生成的赛璐璐（平涂）图，1254×1254 RGBA。**初始实测事实**（决定了一切后续取舍）：

| 观察 | 实测数字 |
|---|---|
| 尺寸 / 颜色数 | 1254×1254 RGBA，1.57 M 像素，44882 种 RGB 三元组（含 alpha 是 70328 种 RGBA 值） |
| 细颗粒噪声 | Immerkaer 鲁棒 σ（只在局部平坦像素取中位数）= **0.209 色阶**；残差 lag-1 自相关 x 0.008 / y −0.107 → **空间白噪声** |
| **云雾状低频斑块（主要视觉问题）** | 48×48 头发窗口 3×3 中值残差只有 0.147，但 luma std 4.92（范围 237–249）→ 低频、成片、肉眼可见 |
| 孤立坏点 | 偏离 5×5 中值 >2 的像素 0.17%、>4 的 0.01%（165 px）→ **不是椒盐噪声**，"去斑点"路线从一开始就不对 |
| 块效应 | 无（帽子黑块按 `x%8` 分布的 \|dx\| 在 0.676–0.766，max/min 1.13）→ 不是 JPEG/DCT 伪影 |
| alpha | 内部 250–254（众数 **253**，不是 255）；轮廓抗锯齿斜坡约 2 px；α≈0 处 RGB 是垃圾（存在 α=1、RGB=(255,255,255) 的散点）→ **统计绝不能让 α<24 的 RGB 参与** |
| 真实软渐变 | 脸颊 y=700 一行 luma 从 229 平滑升到 241，**是 1 色阶台阶但跨度 80+ px** |
| 真实细特征 | 1–2 px 腮红深红笔画（局部对比 12–30 色阶）、1 px 发丝分界、3–8 px 线稿、眼睛高光、帽子白条（对比 137 色阶） |

因此问题的正确定义不是"把所有东西变成纯色"，而是**分类处理**：平坦色块 → 干净的单色；真实软渐变 → 保留；
细特征（笔画/线稿/发丝/高光/AA）→ 逐像素保留；alpha → 内部归一、游离杂点清零、半透明边缘保留原 RGB。

### 1.1 为什么是"保边局部平均"，而不是别的

调研并实测否掉了四条路（完整对照实验与独立审核见第 11.1 节）：

| 方案 | 实测结果 | 结论 |
|---|---|---|
| 分割派：锚点调色板 + k-means + 连通域平坦/渐变判定 + 平面拟合 | 三种阈值变体下"渐变分支"从不触发（nramp = 0），85% 面积落进 detail，已知平坦的帽子窗口全部误判；巨型连通域 + 平面外推错色（182 vs 237） | 否 |
| 矢量化（vtracer → SVG → 重栅格化） | 色块绝对纯（std 0.000）但整图平均改动 2.7–19.4 色阶、细线像素掉约 90%、腮红渐变 202→94 级、alpha 被重写 130 万像素 | 否（是"重画"不是"清洗"） |
| 调色板量化（pngquant / PIL / imagequant） | 平坦区起伏几乎不降（0.2666→0.2555）、渐变压成色带（202→22–95 级） | 否 |
| 通用保边滤波 / 深度学习去噪 | 按真实噪声尺度调 ≈ 什么都不做；调到能去云雾就重画像素（详见第 7 节） | 不足以单独使用 |

**采用的模型**：CIELab 空间里的**范围加权局部均值**（双边/表面模糊），加上三个领域约束：
① 只统计"颜色相近"的邻居（保护边缘、渐变、细线）；② 无偏（不整块压暗/提亮）；③ 可选"整块重刷"处理
**比窗口还大**的低频云雾。

---

## 2. 算法流水线

`src/celclean/pipeline.py::clean(rgba, opts) -> (rgba_out, info)`

| 阶段 | 做什么 | 关键点 |
|---|---|---|
| **S0 alpha 遮罩** | `valid = α ≥ 24`；`lab[~valid] = 0`，使透明区垃圾 RGB 不进入任何统计 | 透明区 RGB 是垃圾（实测 α=1、RGB=255 的散点），必须挡掉 |
| **S1 测噪声** | 用 3×3 拉普拉斯算子的鲁棒中位数（Immerkaer）在"局部平坦"像素上估 σ | 后续阈值都是它的倍数，不写死常数。注意它测的是**颗粒**，测不到云雾（见第 9 节） |
| **S2 保边去噪（核心）** | `bilateral(lab, valid, radius, sigma_range, stride)` | 范围权重 `exp(-d²/2σ_range²)`，`σ_range = 1.5·strength`（dE）。颜色差远 → 权重 0 → **边缘原样**；线性渐变 → 局部均值还是它自己 → **渐变原样**；细线 → 线内像素互相平均 → **保留**。窗口内若无"足够像"的邻居，**保留原像素**（避免孤立点被放大或权重退化） |
| **S3 可选：整块重刷**（`--snap`） | 建"平坦掩膜" → 连通域 → 逐块拟合并重刷 | 局部平均去不掉比窗口更大的云雾，这一步能。掩膜 = `valid ∧ \|lab − 局部均值(r=2r/3)\| ≤ snap_tol`，再做闭运算填洞、按 `aa_band` 腐蚀（保护抗锯齿） |
| **S4 alpha 收尾** | 内部（α≥248 且 3×3 邻域无 <248）→ 255；孤立近零 alpha → 0；α<24 像素**保留原 RGB** | 保留 α<24 的 RGB 是修掉一个真实缺陷：Lab 回程会把它们写成黑色，白底上变成 1166 个浅灰点（最暗 21.4 色阶）。修复后白底渲染最大改动 21.4 → 8.0 色阶、>8 色阶像素 1166 → 0 |

### 2.1 S3 的两种块模型（`--snap-mode`）

- **`plane`（默认）**：每块在 Lab 里最小二乘拟合 `v ≈ offset + dy·(y−cy) + dx·(x−cx)`（逐通道），
  用**相对判据**决定这块是不是"平面"：残差 RMS ≤ `plane_tol`(默认 4.0) × **本图中位块残差**（下限 0.5 dE）。
  拟合残差明显更大的块（曲面阴影、跨进边缘的块）保持不动。相邻块若"偏移量差 ≤ `merge_tol` 且坡度差 ≤ `slope_tol`"
  则合并，合并后重新拟合（最多 3 轮）。最后按 `snap_tol` 把块沿平面**生长**到真实边界。
- **`constant`**：每块用一个平均色（"snap"的原始实现）。实测它会把一条 40 色阶的缓坡**整条刷成一个颜色**
  （合成坡道残差 11.1 色阶 vs plane 的 2.1 色阶 = 与"不刷"一致），**不推荐**，保留仅作对照。

**为什么"相对判据"是必须的**：最初的实现用绝对阈值（残差 ≤ 1.0 dE），结果把**残差最大 = 噪声最大 =
最需要重刷**的块全部拒掉（8 个最平坦窗口的合格数 7/8 → 4/8）。块的残差主体就是"双边没抹干净的那部分云雾"，
所以判据必须相对化。

---

## 3. 参数语义与杠杆方向

| 参数 | 默认 | 语义 | 实测杠杆 |
|---|---|---|---|
| `stride` | **1** | 窗口内采样步长。1 = 全部 tap | **有效**：等耗时下密采样+小半径全面优于稀疏采样+大半径（2.0 s 时起伏/纯色 0.0591/0.528 vs 0.0758/0.496）→ v3 起默认 1 |
| `radius` | 自动 | 窗口半径；`auto = clip(16·√(W·H/1.25M), 3, 24)` | **有效但依赖内容**：强噪声偏向 24–48，弱噪声偏向 8–12；区间内 PSNR 极差 0.18–2.0 dB |
| `strength` | 1.0 | 范围阈值倍数（1.5·strength dE） | **坏旋钮**：1.0→3.0 色块纯度无改善，最坏改动 7.0→21.3 色阶、>8 色阶像素 0→1.29% |
| `snap` | 关 | 是否整块重刷 | **有效但有色斑代价**（第 9 节） |
| `snap_tol` | 0.5 dE | 距局部均值多近才算"平坦" | 收紧（0.15–0.25）可显著降低 snap 的最坏改动（21.4→9.4 色阶），但纯净度也降（平窗 std 0.045→0.11） |
| `min_block` | 64 px | 小于此像素数的块不重刷 | 保护小碎块细节 |
| `aa_band` | 2 px | 块边缘保留不重刷的宽度 | **只在 `snap` 打开时有效**（关掉时输出逐字节相同） |
| `merge_tol` | 1.0 dE | 相邻块颜色差 ≤ 此值则合并成一块 | 防止块间留下 1 色阶平台 |
| `alpha_mode` | normalize | 内部 alpha → 255 | 它改动的像素数**比去噪本身多**（约 77% 像素各 1–2 色阶）；这是"修 AI 脏 alpha"的必要代价，可用 `keep` 关掉 |
| `plane_tol` / `slope_tol` | 4.0 / 0.05 | 平面判据倍数 / 合并坡度容差 | 见 2.1 |

---

## 4. 验收口径（我们怎么量"更好"）

全部由 `src/celclean/qa.py::image_metrics` 产出，`--report` 会写进 JSON。口径为什么这么设计、
以及它曾经漏掉过什么，都在下面。

| 指标 | 定义 | 为什么需要 |
|---|---|---|
| `flat_tiles`（8 个 32×32 最平窗口） | 按**原图**窗口 std 选最平的 8 个，报输出上的 std 均值/中位、std ≤ 0.1 的窗口数、最差颜色偏移 | "该是纯色的地方"的直接证据；按 std 选（不是按残差选）才不会被平滑渐变误选 |
| `local_variation_flat_px` | 只在**原本平坦**的像素上（半径 9 中值残差 < 0.8）算输出的同量 | 不受"选了哪些窗口"影响的全局量 |
| `sigma_grain` | 鲁棒颗粒噪声（Immerkaer 中位数） | 颗粒是否清零 |
| `corr_luma` | 与原图的亮度相关 | 结构/渐变有没有被破坏 |
| `mean / p99 / max_abs_delta_levels` | 逐像素亮度改动（**α≥24 且输出 α≠0** 掩码内） | **有界性**：我们不追求"改得多就是好"，要求最坏改动小 |
| `frac_gt8_levels` | 改动 > 8 色阶的像素占比（同上掩码） | 可见改动的上界 |
| `rendered_*`（v2 新增） | **白底合成、全像素**的 max/p99/mean/>8 色阶 | **口径补漏**：α≥24 掩码会把 α<24 的边缘像素排除在统计之外，而那里曾藏着全图最大的单像素改动（21.4 色阶）。现在两行都给 |
| `max_deltaE_flat_px` | 平坦像素上 99 分位 Lab ΔE | 与人眼感受对齐的补充 |

**曾骗过我们的两件事（记录在案）**：

1. **掩码外的改动**：只看 α≥24 时"最大改动 7 色阶、>8 色阶 0 个"很漂亮，但白底渲染下最大改动是 21.4 色阶、
   1166 个像素 >8 色阶（全是 α<24 边缘被写黑）。已修代码 + 补口径。
2. **用"我们自己的输出"当 ground truth**：半径扫描时 PSNR 会奖励"更像我们自己的那种平滑"，
   于是"越平滑分越高"（r=48 最优），这是度量偏差不是质量结论——因此半径决策改用结构代理指标
   （腮红渐变对比度、细线区峰值梯度、平坦区残余）来判断。

---

## 5. 实测数据

样图 `avator.png`，1254×1254。默认参数（`stride=1`，`snap` 关）：

| 指标 | 数值 |
|---|---|
| 平坦窗口 std（8 个最平窗口） | 0.389 → **0.220**（中位 0.204；3 个窗口 ≤ 0.1） |
| 平坦块颜色偏移（无偏性） | 最差 0.122 色阶 |
| 局部起伏（平坦像素） | 0.2666 → **0.0548（−79%）** |
| 颗粒 σ | 0.2089 → **0.0** |
| 3×3 邻域完全同色像素占比 | 0.182 → **0.554** |
| 亮度相关 | **0.999925** |
| 逐像素改动（去噪域） | 平均 0.634、p99 3.928、**最大 8.0 色阶** |
| >8 色阶像素 | **0.00%**（去噪域与白底渲染都给 0） |
| 白底合成、全像素最大改动 | 8.03 色阶 |
| 耗时 / 峰值内存 | 1254² **3.0 s** / 153 MB；4096² **82.9 s** / 1.0 GB（`--stride 2`：0.9 s / 20.3 s） |

**逐窗口数据（默认档）**：8 个最平窗口 `std_before → std_after` =
0.324→0.055、0.363→0.086、0.400→0.100、0.400→0.191、0.395→0.362、0.406→0.379、0.409→0.375、0.412→0.267。
后 4 个窗口里其实含真实缓坡阴影（std 大是因为坡），**保持不动是有意为之**——这也是"平坦窗口 std 均值"
（0.220）并不低的原因：它被这些含坡窗口拉高，看它不如看 `local_variation_flat_px`（只统计本来平坦的像素）。

**变差函数（不需要拟合的直接诊断）**：`V(d) = ½·E[(x(p) − x(p+d))²]`（全像素中值估计，色阶²）

| d | 1 | 2 | 4 | 7 | 8 | 12 |
|---|---|---|---|---|---|---|
| 原图 | 0.089 | 0.562 | 0.681 | 0.946 | 1.099 | 1.616 |
| 本工具输出 | 0.000 | 0.000 | 0.000 | 0.050 | 0.050 | 0.681 |

读法：原图在小尺度上**不是白噪声**（V(1)≈0.09 是颗粒，之后持续爬升到 1.6 是云雾）；我们的输出在
d ≤ 4 上 V = 0（颗粒与云雾的小尺度成分被压干净），d ≥ 7 的爬升来自**真实结构**（头发/软阴影自身的变化）。

---

## 6. 独立验证：预训练模型交叉验证

用 torchvision 预训练 **ViT-B/16**（`conv_proj` 浅层 patch 嵌入 + 最后一个 encoder block 的 patch token）
与 **ResNet-50**（layer2，1/8 分辨率）对"原图 vs 清理后"提特征。所有 hook 都不改 torchvision 源码。

| 量 | 数值 | 解读 |
|---|---|---|
| 浅层 patch 余弦 | mean 0.99930 / **min 0.99357** / `<0.99` 0/196 | 局部纹理、边缘、笔画没被动 |
| 深层 patch 余弦 | mean 0.99262 / 中位 0.99860 / `<0.99` 28/196 | 深层语义整体保持；掉下去的集中在云雾区 |
| CKA（尺度无关的全局保真） | conv **0.99989** / deep **0.98143** | —— |
| 恒等映射（patch 是否仍最像自己） | deep **193/196**，唯一实质失配是帽子高光旁的云雾 | 结构没有"搬家" |
| **纯去噪分量**（保留原 alpha） | deep CKA **0.99721** | 比 JPEG q90（0.99522）**更轻** |
| alpha 归一化分量 | deep CKA 0.98719 | 比 1 px 全局平移（0.98951）还重 |

**校准（回答"0.999 算高吗"）**：±1 色阶抖动 5% 像素就能把深层相似度打到 0.99995，所以 0.999 不是高门槛；
按改动幅度对齐（同 mean|Δ| 的零均值噪声 σ=1.42）深层 CKA 是 0.99871 ≫ 我们的 0.98143 —— 因为我们的改动是
**成片结构化的**（集中在云雾与 AA 带），而噪声逐点相互抵消。分区域归因（像素域）：平坦内部 79% 的像素，
改动 1.014 色阶里 **0.900 来自 alpha 归一化、RGB 只占 0.350（且零均值）**；边缘（13.5% 像素）才是 RGB 去噪主场
（1.452 色阶）。

**这套验证同时抓出一个真缺陷**（α<24 边缘被写黑，第 2 节 S4），并证明我们自己原来的验收掩码会把它藏起来。

---

## 7. 与同类工具 / 通用去噪的实测对比

### 7.1 通用去噪与"真 DL"去噪（34 个参数档，含 DnCNN / FFDNet 推理）

| 工具（选中档） | 平坦窗口 std | 平坦像素起伏 | 渐变保真 | >8 色阶像素数 | 平坦块最大改动 |
|---|---|---|---|---|---|
| **celclean 默认** | 0.227 | 0.087 | 保留 | **0** | **4.7 色阶** |
| FFDNet σ=6 / σ=10 | 0.19 / 0.17 | — | 保留 | 1031 / 2482 | 9.4 |
| DnCNN（按噪声尺度标定） | 0.381 | 比原图**更差 4%** | 保留 | 0 | 小 |
| skimage NL-means h=3 | 0.261 | **0.068** | 保留 | 701 | 8.4 |
| cv2 NL-means h=6 | 0.199 | 0.097 | 保留 | 21424 | 23.8 |
| skimage TV w=12 | — | — | 台阶化 | 19482 | 14.9 |
| cv2 双边 σc=50 | — | — | 过冲 | 18413 | 12.9 |
| 高斯 σ=1.5 px | — | — | 糊 | 85843 | 57.6 |
| guided r=16 | 0.34 | 0.19 | 保留 | **529315（42% 像素）** | 180 |

> 注：这一组对照是在 `stride 2` 工作点上跑的（第三方工具的测量也都在同一批次），所以 "celclean 默认"
> 行的平坦窗口 std 是 0.227、起伏 0.087 —— 对应的是 v2 的默认档；v3 起默认 `stride 1`，同口径数字为
> 0.220 / 0.0548（第三方各档仍可用同一 `qa` 口径复跑，脚本见 `_probe/oss/`）。

**核心结论：没有安全中间档。** 按这张图的真实噪声尺度（0.2 色阶）标定，它们几乎不动手；想真的去掉云雾，
就必须重画像素。最像我们的对手是 `skimage` NL-means h=3（起伏 0.068 甚至更好），但它平坦块 std 输（0.261）、
仍有 701 个像素改动 >8 色阶、不处理 alpha、慢（4.8 s vs 1.0 s@stride2）。
**顺带一个方法学警告**：BRISQUE 这类自然照片上的无参考质量分在这类图上**方向相反**（把带噪原图判为最好，
把任何真去噪判为更差），不能用来评价平涂图。

### 7.2 同类开源工具（41 个候选调研，19 种工具 / 34 档实测）

| 路线 | 代表 | 结果 |
|---|---|---|
| 矢量化 → 重栅格化 | vtracer（7.1k★, MIT） | 色块 std **0.000（8/8 绝对纯）**；但相关 0.926–0.995、平均改动 2.7–19.4 色阶、最大 190–200、**细线像素 6.25%→0.54%（掉约 90%）**、腮红 202→94 级、alpha 重写约 130 万像素 |
| AI 动漫去噪/超分 | waifu2x-ncnn（3.5k★）/ Real-ESRGAN | waifu2x 最忠实（mean 0.39–0.48、p99 1.00）但**平坦起伏反而变差**（0.2666→0.2843/0.3120）、颗粒 σ 不变 → 对"纯色块脏"无效；Real-ESRGAN 色块偏移 4.7 色阶、会加假边 |
| 保边平滑 | OpenCV / scikit-image | 与我们同源但**没有块语义**（不做块内归一、AA 带保护、块合并）。`cv2.bilateralFilter(d=21,σ=10)` 是最接近的对手：起伏 0.0445（优于我们）、纯色占比 0.462（≈ 我们 0.463），但最坏改动 10.5 > 8.0、不处理 alpha |
| 调色板量化 | pngquant / PIL / imagequant | 反向工具：渐变压成 22–95 级色带，平坦起伏几乎不降（云雾被映射到相邻调色板项） |
| 线稿/平涂清理 | Anime2Sketch 等 | **图片侧"赛璐璐清理"是生态空白**（`cel shading anime` 命中的全是游戏 shader）；这些项目是"从彩图反解线稿"，与清理色块正交 |

**我们在哪赢**：最坏改动有界且最低（8 色阶、>8 色阶 0 个）；同时做到"清掉颗粒 + 5 轴 Pareto 前列"；
唯一处理 alpha 语义；零下载、零模型、确定性。
**他们在哪赢**：vtracer 的绝对纯净与可再编辑（SVG）我们做不到；平整度天花板（`cv2` 双边 d=41、NL-means h=3）
略高于我们；`cv2.guided` 的忠实度（max 4.3 色阶）更高但基本不去噪；纯速度比我们快（0.1–1.2 s）。

---

## 8. 性能

| 步骤 | 1254² v1 → 现在 | 4096² v1 → 现在 |
|---|---|---|
| 总计 `clean()` | 6.0 s → **1.0 s**（stride 2）/ 3.0 s（stride 1，默认） | 210.5 s → **20.3 s**（stride 2）/ 82.9 s（stride 1） |
| 其中 `bilateral` | 5.71 s → 0.84 s（占 90% → 同样占大头） | 202.7 s → ~20 s |
| 峰值内存 | 237 → **153 MB** | 2131 → **1025 MB** |

**根因**：原实现对每个 tap 都分配全图规模的临时数组（4096² 每 tap 三块 201 MB），单 tap-px 成本因此劣化 4.3×。
**手法**：行带（cache blocking）+ 带级线程（`run_bands`，带间无依赖）、带高按 h/w/核数自适应、
缓冲复用与 `out=` 就地融合、256 项 sRGB→线性 LUT（uint8 入参下按位等价）、`srgb↔lab` 与 `robust_sigma_luma` 分带化
（后者原先把 9 个 (H−2,W−2) 平面 stack 起来，4096² 曾要 1.8 GB）、alpha 判定 `box_count==0` → 纯布尔 OR。

**逐位等价证明**：与优化前的冻结副本做 26 项函数级 + 整条 `clean()`（`snap` 开/关）对比，
优化后 **max|Δ| = 0、0 个像素不同、PNG 字节相同**（α≥24 掩码内，1254² 与 4096² 都验过）。
被否掉的优化：exp 的 LUT（更慢）、`np.sum(axis=-1)` 版 d2（更慢）、不分带只做 `out=`（无收益）、
镜像 tap 复用（halo 开销 ≥ 节省，已量化）、scipy 距离变换（收益 ~0 且禁止新增依赖）。

---

## 9. 已知失效模式与边界

1. **8-bit 色带（banding）**：去噪会把紫外的台阶显出来——原图的颗粒恰好充当抖动，颗粒被去掉后，缓坡
   还原成 1 色阶台阶（腮红区 ≥12px 平台面积 4.5% → 56%、硬台阶 27 → 220）。**"绝对纯色块"与"无台阶"在亚色阶
   区域数学上互斥**（真值落在两个色阶之间时必须二选一）。我们默认选纯净；`plane` 的 snap 能缓解缓坡台阶，
   但它带来第 2 条的代价，所以默认不开。
2. **`--snap` 的硬边色斑**：块分割是启发式的，不等于美术的真实分区。plane/constant 两种模式都会在大片柔和
   渐变上留下 1–2 色阶的成块差异——**放大 + 对比拉伸下肉眼可见**。全局指标反而更"漂亮"
   （3×3 纯色 0.638、起伏 0.0427），这是"聚合指标看不见块结构伪影"的典型例子，因此默认关闭。
3. **单张图测不准噪声尺度**：我们试了三种方法（自相关、阈值选样 + 均方差、无选择中值变差函数），
   全部无法可靠分离"噪声"与"结构"（自相关被块间色差主导 → 所有输入都报 L≈15；阈值选样把振幅压低 7×；
   变差函数能看出形状但拟合不出 L）。所以 `auto_radius` 只能按图像尺寸缩放，**不要**据此做更复杂的自适应。
   另外 `sigma_grain`（0.209）只反映颗粒，**测不到云雾**（实测云雾振幅约 1.3 色阶）。
4. **半径依赖噪声振幅而非相关长度**：强噪声（σ≈2.2）偏向 r=24–48，弱噪声（σ≈0.7）偏向 r=8–12
   （12→32 会掉约 1.1 dB）。噪声轻的图请手动 `--radius 8~12`。
5. **`--strength` 不是"更干净"旋钮**（见第 3 节）。
6. **`--aa-band` 在 `snap` 关闭时是死参数**（输出逐字节相同）。
7. **孤立高对比坏点无法与真实高光区分**：判据在这张图上 91% 命中贴线稿/发丝的合法像素，松版会误伤 1/6
   真高光并把 max|Δ| 从 7 推到 209 色阶 → 不做自动清除。
8. **所有默认值都标定在一张图上**（1254×1254 AI 平涂）。换风格/尺寸前建议 `compare` 目视确认。

---

## 10. 复现

```bash
cd "C:/Users/ghost/Desktop/paint bucket"

# 基本用法
uv sync
uv run celclean clean avator.png --report        # 输出 + 验收报告 JSON
uv run celclean compare avator.png --out-dir examples
uv run pytest -q tests                           # 18 项测试

# 各档位
uv run celclean clean avator.png --stride 2 -o fast.png
uv run celclean clean avator.png --radius 20 -o cln.png
uv run celclean clean avator.png --snap -o pure.png            # plane 模型
uv run celclean clean avator.png --snap --snap-mode constant -o pure_const.png   # 仅作对照
```

**原始实验留档（本地目录 `_probe/`，未纳入 git，因为体积/一次性）**：

| 目录 | 内容 |
|---|---|
| `_probe/perf/` | 分阶段计时、带高扫参、逐位等价校验、4096² 输入与原始 JSON |
| `_probe/dl/` | ViT/ResNet 交叉验证（脚本、`results.json`、热图/散点/最差 patch 裁图） |
| `_probe/dlbase/` | 34 个通用/深度学习去噪基线的全部指标与裁图 |
| `_probe/oss/` | 41 候选调研 + 19 工具 34 档实测（7 张自动生成的数字表 + 裁图 + 复现脚本） |
| `_probe/enhance/` | 三条增强原型的验证（去色带 / 边缘 AA 重建 / 坏点清除）及其否决依据 |
| `_probe/calib/` | 半径与噪声尺度校准（合成退化 + 真值恢复 + 无偏过平滑探针） |
| `_probe/v3/` | v3 决策依据：块统计、合成坡道、snap A/B、护栏扫描、目视对照图 |

各目录内的 `REPORT.md` 是完整报告（含原始数字、复现命令、反例）。
被否掉的方案、独立审核结论与各版本变更记录见第 11 节。

---

## 11. 设计演进与变更记录

本节是"为什么长这样"的档案：早期对照实验、外部独立审核（subagent `RevB`）的结论、以及 v1→v2→v3 的变更。
原始留档在 `_probe/`（本地目录，未纳入 git）：`_probe/revB/REPORT.md`、`_probe/*/REPORT.md`。

### 11.1 方案对照实验（v1 之前，逐条否掉）

| 候选 | 实测结果 | 结论 |
|---|---|---|
| A. 只用去噪滤波（中值/双边/引导，大半径） | 帽子云斑被抹平，但 r=16 引导滤波把脸颊软阴影抹成暗团；细笔画旁出现暗晕 | ❌ |
| B. 全局调色板（Lab k-means + ICM 空间分配） | 脸颊渐变**台阶化**（渲染相关 0.089，阶梯 238/231/240）；k=64 仍有 5.4% 像素 ΔE>3 | ❌ |
| C. 区域分割 + 每区域"平坦/渐变"判定 + 平面拟合 | 渐变与细特征能保住，但 k-means+ICM 标签把平坦块与相邻渐变并成同一区域（155 个区域里只有 20 个判为平坦）；改进标签后颜色误差降到 ≤0.4 色阶，却产生 231981 px 的**巨型组件**，且平面外推会给出错误颜色（渲染值 182 vs 真值 237） | ❌ 作为主路径 |
| D. **CIELab 双边/表面模糊（小 range σ）** | 平坦块**无偏**（7 个最平窗口偏差 ≤0.25 色阶）；云雾 proxy 0.266→0.051（−81%）；渐变相关 0.9978/0.9995/0.9988；细特征变化 1–3 色阶；>8 色阶改动 2.18% 面积（几乎都在边缘） | ✅ **采纳为核心** |
| E. 学习型去噪 / 扩散重绘 | 非确定性、改变画面、依赖重模型 | ❌ |

**独立审核（subagent RevB，它拿到的是被否掉的"分割派 v2"并独立实现复现）**：

1. **v2 的"平坦/渐变/细节"三分类规则是坏的**：三种阈值变体下 `nflat≈27–33 / nramp=0 / ndetail≈167–183`，
   **渐变分支从不触发**，85% 面积落进 detail，4 个已知平坦的帽子窗口**全部**落在 detail——这是改用双边核心的直接证据。
2. **平坦↔平坦边界**直接吸附到常数色会让 23.8% 的边界像素 ΔE>2（mean 1.41）；两色覆盖混合后降到 2.7%（mean 0.69）。
   混合必须在 **Lab** 里解（Lab 0.694 < linearRGB 0.731 < sRGB 0.718）；扁平边界里只有 10 个像素是 3 色以上交汇 → 2 色模型够用。
3. **边界规模**：全图边界像素 60490（4.85%），其中平坦↔平坦 7379，真正是 AA 混合色的 2310 px。
4. **σ 的定义口径差异会让所有阈值漂移 6.6×**（同一张图在"整图 / 腐蚀内部 / Lab vs 8bit"三种口径下 σ 在 0.208–1.380 之间，
   腮红笔画位移随之 0.54/1.07/2.90 色阶）→ 实现里把 σ 固定为"掩码内 Immerkaer 中位数、单位取滤波器工作空间（Lab L）"，
   并且 Lab 三个通道各自一个 eps。
5. **双边核心被独立复现**：cheek 相关 0.99736、云雾 proxy 0.4305→0.1108（−74%）、>8 色阶改动 0.0%——与本地结果同量级、方向一致。

### 11.2 v1 → v2：性能与缺陷修复（多方向复核之后）

- 性能：1254² **6.0 s → 1.0 s**、4096² **210.5 s → 21.3 s**，逐位等价（26 项函数级 + 整条流水线对比，max|Δ|=0、PNG 字节相同），
  峰值内存 237→153 MB / 2131→1025 MB。手法与被否掉的优化见第 8 节。
- 缺陷（由独立 DL 取证发现）：**α<24 边缘像素的 RGB 被写黑**（白底最暗 21.4 色阶、1166 px >8 色阶，且被旧验收掩码完全遮蔽）→ 保留原 RGB + 补"白底全像素"口径 + 回归测试。
- 顺带修掉 `masked_box_mean` 的 2-D 广播隐患（(h,w,h) → 1254² 要 7.35 GiB，会被 OS 静默杀掉）。
- 三条效果增强原型（去色带 / 边缘 AA 重建 / 孤立坏点清除）验证后**不合并**，依据见 `_probe/enhance/REPORT.md`。

### 11.3 v2 → v3：以质量换时间

- 默认 `stride` 2 → **1**（等耗时实测：密采样+小半径全面优于稀疏采样+大半径；纯度提升且不动渐变）。
- `--snap` 的默认模型换成 **plane**（常数模型会把一条缓坡整条刷平）；平面判据改为**相对中位残差**（绝对阈值恰好拒掉了最该重刷的块）。
- 仍不把 `snap` 设为默认：两种块模型都会在大片柔和渐变上留下硬边色斑（目视验证），尽管全局指标更"漂亮"。
- 被数据否掉并不再尝试的方向：半径按"测得的噪声相关长度"自适应、`--strength` 当纯度旋钮、H1/H2/H3 三个增强原型（详见第 9 节与 `_probe/enhance/REPORT.md`）。
