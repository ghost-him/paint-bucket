# 赛璐璐风格 AI 出图「去噪 / 重绘纯色」工具 — 最终技术方案

> 目标文件：`head4.png`（1254×1254，RGBA）。工具暂名 `celclean.py`。
> 本文所有数字均为在本机对该图实测（脚本见 `_probe/` 与 `_probe/mine/`）。

---

## 一、任务背景（先量化问题，再定方案）

### 1.1 图像实测事实

| 观察 | 测得的数字 |
|---|---|
| 尺寸/模式 | 1254×1254 RGBA，1.57 M 像素，只有 44882 种 RGB 三元组 |
| 细颗粒噪声 | Immerkaer 鲁棒 σ（只在局部平坦像素上取中位数）= **0.209**（±1 色阶）；残差 lag-1 自相关 x 0.008 / y −0.107 → **空间白噪声** |
| 孤立坏点 | 偏离 5×5 中值 >2 的像素 0.17 %，>4 的 0.01 %（165 px）→ 不是椒盐噪声 |
| **云雾状低频斑块（主要视觉问题）** | 48×48 头发窗口 3×3 中值残差仅 0.147 但 luma std 4.92（范围 237–249）；对比拉伸（20..60 → 0..255）的帽子黑块呈现明显云斑 |
| 块效应 | 无：帽子黑块 `|dx|` 按 `x%8` 分布 0.676–0.766（max/min 1.13），无周期性 |
| alpha | 内部 250–254（众数 253）而**非 255**；轮廓 AA 斜坡约 2 px；alpha≈0 处 RGB 是垃圾（存在 a=1、rgb=(255,255,255) 的散点）→ 统计时绝不能把 α<24 的 RGB 计入 |
| 真实软渐变 | 脸颊 y=700 一行 luma 从 229 平滑升到 241（**1 色阶台阶**、跨度 80+ px）；§3.2 的 20 像素剖面显示 G 通道 227→219→240 |
| 真实细特征 | 1–2 px 深红腮红笔画（局部对比 12–30 色阶）、1 px 发丝分界、3–8 px 线稿、眼睛高光；帽子白条与黑面的台阶对比 137 色阶 |

### 1.2 因此，问题的正确定义

不是「把所有东西变成纯色」，而是**分类处理**：

1. **平坦色块** → 变成干净的单一颜色（用户的核心诉求）；
2. **真实软渐变**（脸颊腮红、头发/帽子软阴影）→ 必须保留（做不到完美可接受）；
3. **细特征**（腮红笔画、线稿、发丝、高光、AA 边缘）→ 必须逐像素保留；
4. alpha 通道 → 内部压到 255、边缘斜坡去噪、透明区 RGB 清零。

---

## 二、技术方案选择（做过对照实验，逐条否掉）

| 候选 | 实测结果（本图） | 结论 |
|---|---|---|
| **A. 只用去噪滤波**（中值/双边/引导滤波，大半径） | `proto1`：帽子云斑被抹平，但 r=16 引导滤波把脸颊软阴影抹成暗团；细笔画旁出现暗晕 | ❌ |
| **B. 全局调色板**（Lab k-means + ICM 空间分配） | `proto2`：脸颊渐变被**台阶化**（渲染 corr 0.089，阶梯 238/231/240）；k-means 表 k=64 仍有 5.4 % 像素 ΔE>3 | ❌ |
| **C. 区域分割 + 每区域平坦/渐变判定** | `proto3/4`：渐变保住（corr 1.000）、细特征保住，但 k-means+ICM 标签把平坦块与相邻渐变并成同一区域 → 155 区域里只有 20 个判为平坦，帽子块没被清干净；改进标签（小半径 majority vote + 小连通域吸收）后平坦块颜色误差降到 ≤0.4 色阶，但 20 个锚点太粗产生 **231 981 px 的巨型组件**，且「ramp → 平面拟合」会在组件内**外推出错误颜色**（实测渲染值 182 vs 真值 237） | ❌ 作为主路径 |
| **D. CIELab 双边/表面模糊（小 range σ）** | `_probe/mine/bilateral.py`，R=12、σ_range=1.5 色阶：<br>• 平坦块**无偏**：7 个最平坦 32×32 窗口的颜色偏差 ≤ **0.25** 色阶<br>• 云雾 proxy（平坦像素上的半径 9 中值残差）**0.266 → 0.051（−81 %）**<br>• 渐变保真：脸颊/头发/帽子行 corr **0.9978 / 0.9995 / 0.9988**（ptp 39.2→37.9）<br>• 细特征：腮红 218→221、眼睛高光 241→239、帽子白条 AA 58/74→**61/72**、线稿 70→69<br>• 全图 >8 色阶改动仅 2.18 % 面积（几乎都在边缘） | ✅ **采纳为核心** |
| **E. 学习型去噪 / 扩散重绘** | 非确定性、改变画面、依赖重模型 | ❌ |

**最终方案 = D（双边表面模糊）为核心 + 可选「cel 断色」snap + AA/alpha 收尾。**

理由：D 不做分割、逐像素局部、无偏（不会像引导滤波那样在小结构上偏色 4–13 色阶），
天然同时满足「平坦块干净」与「渐变/细特征保留」这两个相互冲突的要求；
用户的「取色块平均色重绘」直觉被它更稳健地实现了（用局部同色加权平均，而不是硬取整块均值，
因此不会把相邻不同色的像素平均进来）。

---

## 三、最终流水线（每步都可单独关闭）

```
S0 读取 + 掩码      alpha>=24 才算有效像素；无效区 RGB 不参与任何统计
S1 噪声尺度         σ_grain = 局部平坦像素上的中值 Laplacian 估计（实测 0.209）
                    σ_mottle = 平坦像素上「半径 17 中值残差」的 MAD（实测 ≈0.3–1.0）
                    所有阈值都写成 σ 的倍数，不写死
S2 双边核心（默认开）Lab + 双边滤波：空间半径 R=12（按 1 MP 归一），range σ=1.5 色阶
                    → 去掉颗粒与云雾斑块；台阶、渐变、细笔画、AA 各自保留
S3 cel 断色 snap（可选，**默认关**）只对「局部 17×17 内变化 <1.2 色阶」的像素生效：
                    吸附目标必须是**局部块均值**（先用局部同色加权均值求出该块颜色，再整块吸附），
                    **不要用全局调色板**：实测全局 24 色调色板会在个别块产生 +1.41 色阶偏差，
                    6.4× 拉伸下变成肉眼可见的淡斑（见 README 的 `examples/detail-3-*.png` boost 行）。
                    snap 的作用是把「近似平坦」变成「精确纯色」，属于观感取舍，故默认关。
S4 边缘/AA 收尾     **默认不做混色**：边界像素直接沿用 S2 的双边结果（实测双边保留了原始 AA：
                    帽子白条 58/74 → 61/72）。只有当 S3 的 snap 开启时，才在**两侧同类（平坦↔平坦）**
                    的边界做两色覆盖混合；实测该人群只有 937 px（0.06%），混合误差 0.69 dE，
                    而直接吸附是 1.41 dE。**两侧异类（平坦↔渐变/细节，占 87%）禁止混色**：
                    RevB 实测这类 2310 px 的两色拟合误差 4.95 dE（74% >2 dE），比什么都不做（2.58 dE）更差。
S5 alpha 通道       内部（到边缘距离 >1）→ 255；边缘保留原斜坡并做 3×3 中值去噪；
                    全透明像素 RGB → 0（防白边）
S6 输出与 QA        out.png（RGBA 8-bit，可选 16-bit）+ 报告：组件/解析图、ΔE 热图、
                    原图/结果 2×–4× 并排裁图、JSON 统计
```

### 3.1 CLI 与参数

```
uv run --no-project --with pillow --with numpy --with scipy python celclean.py head4.png \
    --out head4-clean.png \
    --strength 0.5        # 0=只去颗粒 / 0.5=默认 / 1.0=最狠（映射 σ_range 1.0→3.0 色阶 与 R 8→16）
    --snap on|off         # 是否做 cel 断色（默认 on）
    --palette 24          # snap 调色板大小
    --keep-gradient on    # 渐变保护（局部变化阈值），on=默认 1.2 色阶
    --alpha-normalize on  # 内部 alpha 是否压到 255
    --report              # 输出 QA 文件
```

### 3.2 验收标准（可测）

**先钉死测量协议**（否则两边数字对不上：我用「块均值 − 同窗口原图中值」得到偏差 ≤0.25 色阶，
RevB 用另一套口径测同一张图得到 −0.30…−0.78 色阶；全局改动我用「对局部中值」得到 2.18%，
RevB 用「像素对像素」得到 0.0%/max 7.21 色阶——后者才是对的，我的口径在边缘虚高）：

- 平坦块窗口：**左上角**取 (1088,224)、(416,512)、(672,864)、(336,768)、(1152,1152)，尺寸 32×32；
- 颜色偏差：窗口内**亮度均值(渲染) − 窗口内亮度中值(原图)**；
- 全局改动：**像素对像素 |Δluma|**，不是对局部中值。

| 指标 | 合格线 |
|---|---|
| 平坦块颜色偏差（上述 5 窗口） | ≤ 0.5 色阶 |
| 平坦块残余 std | 最平的 3 个窗口 ≤0.10；含真实缓坡阴影的窗口允许保留（报告按窗口逐个列出，避免用均值掩盖） |
| 云雾 proxy（平坦像素半径 9 中值残差） | ≤ 0.12（原 0.266–0.43） |
| 渐变行相关（脸颊 y=700 / 头发 y=860 / 帽子 y=250） | ≥ 0.99 |
| 细特征对比保留（腮红笔画、眼睛高光、发丝分界、帽子白条、线稿，各 3 个采样点） | 对比变化 ≤ 5 色阶且位置不偏移 >1 px |
| 全局像素对像素改动 | >8 色阶的像素为 0（实现后实测 max 7.0，>8 色阶 0%） |
| 平坦块是否真的变纯色 | 记 `f_flat`（被判平坦的面积占比）与窗口 std，**不得用「看起来更干净」当验收** |
| 运行时间 | 1254² ≤ 5 s；4K（8.3 Mpx）≤ 20 s（RevB 实测 1× 全流程 2.3–3.6 s） |

### 3.3 语言与实现

- **Python 3 + numpy + Pillow（scipy 可选），用 `uv run --no-project --with ...` 直接跑**。
  理由：整条流水线是「积分图 box filter / 加权平均 / 小规模 k-means」这类数值运算，
  numpy 几行就能表达；本机 `python.exe` 不在 PATH，`uv` 已验证可直接跑且零安装；
  1.57 M 像素最慢的一步（朴素双层循环写不出、必须向量化的双边）用
  **bilateral grid（O(N) 的 splat–blur–slice 近似）**实现，可线性扩展到 4K。
- 不用 node（没有 numpy 等价物，数值代码会难写难调）；
  Rust 作为**后续可选**：算法稳定后可作为单文件 exe 的移植目标（不是第一版）。

### 3.4 主要风险与对策

| 风险 | 证据 / 对策 |
|---|---|
| 双边把「真实软阴影」当噪声抹平 | 实测相关 0.998+，σ_range 仅 1.5 色阶就能吃掉云雾；`--strength` 可回退 |
| 1–2 px 细笔画被削弱 | 实测腮红/线稿/白条保留在 1–3 色阶内；但**必须加一条硬约束**：`|B − median_r4(B)| > 3σ` 的像素永不吸附（细结构保护地板，否则「更平」的调参就会吃掉笔画） |
| snap 引入低幅色斑 | 实测全局调色板在个别块偏 +1.41 色阶，6.4× 拉伸可见；对策：吸附到**局部块均值**而非全局调色板，且默认关 |
| σ 估计口径不定会让所有阈值漂移 | RevB 实测同一张图 σ 在 0.208–1.380 之间（整图/腐蚀内部/Lab vs 8bit 单位，6.6×），腮红笔画位移随之 0.54/1.07/2.90 色阶；对策：σ 定义为「腐蚀内部掩码上逐块 Immerkaer 的中位数，单位取滤波器工作空间（Lab L）」，并且 **L/a/b 各自一个 eps**，再做 ±30% 敏感性复测 |
| alpha 压 255 改变非白底观感 | 253→255 在深底上 +2.0 色阶（0.78%）；但 12% 的内部像素在 250–252，最多 +5 色阶（2.0%）——比「<1%」更严重；提供 `--alpha-normalize off`；α∈{1,2} 的 34536 像素单独吸附到 0 |
| 大图性能 | 朴素 441 taps 双边 1.57 MP 要 33 s；用积分图/O(N) 结构（guided 型权重或 bilateral grid）后 4K ≈ 12–20 s（RevB 独立外推），Python 足够，不需要 Rust |

---

## 三·补、独立审核（新 subagent `RevB`）的结论与对方案的修正

`RevB` 拿到的是**旧版方案 v2**（分割派），它独立实现了 v2 并实测，结论如下（数据在
`_probe/revB/exp1..4.json`，它写完正文前进程退出，数字是原始的）：

1. **v2 的「平坦/渐变/细节」三分类规则是坏的**：三种阈值变体下
   `nflat≈27–33 / nramp = 0 / ndetail≈167–183`，即 **ramp 分支从不触发**，85 % 面积落进 detail，
   4 个已知平坦的帽子窗口**全部落在 detail**（`exp1.json` / `exp2.json`）。
   → 这就是我随后放弃分割派、改用双边核心的直接原因（与 `proto9` 实测的巨型组件、
   平面外推错色 182 vs 237 相互印证）。
2. **平坦块边界的抗锯齿必须单独处理**：平坦-平坦边界上，直接吸附到常数色会让
   **23.8 % 的边界像素 ΔE>2**（mean 1.41）；用两色覆盖混合后降到 **2.7 %**（mean 0.69）。
3. **混合必须在 Lab 里解**：Lab mean 0.694 / p90 1.298 < linearRGB 0.731 < sRGB 0.718；
   Lab 最优（`exp4.json`）。扁平边界里只有 **10 个像素**出现 3 色以上交汇 → 2 色模型够用，
   但仍要留一个「三色交汇」兜底（就近取两色、其余按面积加权）。
4. **边界规模**：全图边界像素 60 490（4.85 %），其中平坦-平坦边界 7 379，真正是 AA 混合色的
   2 310 px（31 %）→ S6 是**必须**的一步，不是可选优化。
5. **alpha**：众数 253、只有 0.09 % 像素为 255；轮廓带 75 381 px（5.8 %）。压到 255 的影响已量化，
   可安全作为默认（提供开关）。
6. **双边核心被独立复现**：`exp2.json` 的 bilateral 段给出 cheek 相关 0.99736、
   mottle proxy 0.4305→0.1108（−74 %）、高光/腮红/白条偏差 0.72 / −0.07 / 0.14、
   >8 色阶改动 **0.0 %** —— 与我自己的 −81 % / 2.18 % 同量级，方向一致。

**修正后**：v2 的 S3–S5（锚点调色板 + 连通域平坦/渐变判定 + 平面拟合）**整体删掉**，
换成二、三节的「双边表面模糊 + 可选 snap + 边界覆盖混合（Lab）」。

---

## 四、需要你拍板的两点

1. **snap 默认关**（我的建议）。理由：双边已把云雾压到「视觉上已经是干净纯色」（像素对像素
   最大只差 7 色阶、多数平坦块残余 <0.15 色阶），而 snap 会带来 1 色阶级别的局部色斑
   （拉伸才可见，但你自己看图时可能会觉得「怎么多了块灰斑」）。要「绝对纯色」再开即可。
   开的话我会按上面说的改成吸附到局部块均值，而不是全局调色板。
2. **输出尺寸/格式**：保持 1254×1254 8-bit RGBA（默认）；要不要顺带给 2× 放大或不透明白底版？

确认后我就按第三节写 `celclean.py`（预计 300–400 行，含 QA 报告），并用 3.2 的协议自测一遍给你看图。
